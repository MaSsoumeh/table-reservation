import SearchBar from "@/app/components/SearchBar";

const Header = () => {
  return (
    <div className="bg-gradient-to-r from-primary to-gray-dark p-2">
      <SearchBar />
    </div>
  );
};

export default Header;
